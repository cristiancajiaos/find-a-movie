import { UpcomingMovieDates } from './upcoming-movie-dates';

describe('UpcomingMovieDates', () => {
  it('should create an instance', () => {
    expect(new UpcomingMovieDates()).toBeTruthy();
  });
});
