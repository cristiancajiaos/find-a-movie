import { ResponseNowplayingMovie } from './response-nowplaying-movie';

describe('ResponseNowplayingMovie', () => {
  it('should create an instance', () => {
    expect(new ResponseNowplayingMovie()).toBeTruthy();
  });
});
