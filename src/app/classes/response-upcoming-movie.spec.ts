import { ResponseUpcomingMovie } from './response-upcoming-movie';

describe('ResponseUpcomingMovie', () => {
  it('should create an instance', () => {
    expect(new ResponseUpcomingMovie()).toBeTruthy();
  });
});
