import { ResponsePersonResult } from './response-person-result';

describe('ResponsePersonResult', () => {
  it('should create an instance', () => {
    expect(new ResponsePersonResult()).toBeTruthy();
  });
});
