import { ResponsePersonMovieCredits } from './response-person-movie-credits';

describe('ResponsePersonMovieCredits', () => {
  it('should create an instance', () => {
    expect(new ResponsePersonMovieCredits()).toBeTruthy();
  });
});
