import { ResponseMovieResult } from './response-movie-result';

describe('ResponseMovieResult', () => {
  it('should create an instance', () => {
    expect(new ResponseMovieResult()).toBeTruthy();
  });
});
