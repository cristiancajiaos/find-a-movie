import { ResponseSearchMovie } from './response-search-movie';

describe('ResponseSearchMovie', () => {
  it('should create an instance', () => {
    expect(new ResponseSearchMovie()).toBeTruthy();
  });
});
